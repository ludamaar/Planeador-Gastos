# Planeador-Gastos

Planeador de gastos (aplicación en `planeador.html`).

## Memoria de Claude Code

Este repo tiene habilitada la memoria persistente **claude-mem** para las
sesiones de Claude Code. Consulta [`.claude/MEMORY.md`](./.claude/MEMORY.md)
para la puesta en marcha (una sola vez: `npx claude-mem install`).
